<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.mapper.PostMapper">

    <!-- tb_post 테이블 전체 컬럼 -->
    <sql id="postColumns">
          id
        , title
        , content
        , writer_id as writerId
        , view_cnt as viewCnt
        , notice_yn as noticeYn
        , delete_yn as deleteYn
        , created_date as createdDate
        , modified_date as modifiedDate
    </sql>


    <!-- 게시글 저장 -->
    <insert id="save" parameterType="PostDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_post (
            title
            , content
            , writer_id
            , view_cnt
            , notice_yn
            , delete_yn
            , created_date
            , modified_date
            , password
            , board_id
        ) VALUES (
            #{title}
            , #{content}
            , #{writerId}
            , 0
            , #{noticeYn}
            , 0
            , now()
            , now()
            , #{password}
            , #{boardId}
        )
    </insert>


    <!-- 게시글 상세정보 조회 -->
    <select id="findById" parameterType="long" resultType="com.study.domain.PostDTO">
        SELECT
            <include refid="postColumns" />
        FROM
            tb_post
        WHERE
            id = #{id}
    </select>




    <!-- 게시글 수정 -->
    <update id="update" parameterType="com.study.domain.PostDTO">
        UPDATE tb_post
        SET
              modified_date = NOW()
            , title = #{title}
            , content = #{content}
            , notice_yn = #{noticeYn}
        WHERE
            id = #{id}
    </update>


    <!-- 게시글 삭제 -->
    <delete id="deleteById" parameterType="long">
        UPDATE tb_post
        SET
            delete_yn = 1
        WHERE
            id = #{id}
    </delete>


    <!-- 게시글 리스트 조회 -->
    <select id="findAll" resultType="com.study.domain.PostResponse">
        SELECT
            <include refid="postColumns" />
        FROM
            tb_post
        WHERE
            delete_yn = 0
        ORDER BY
            id DESC
    </select>

    <!-- 게시판 별 게시글 리스트 조회 -->
    <select id="findByBoardId" parameterType="long" resultType="com.study.domain.PostDTO">
        SELECT
        <include refid="postColumns" />
        FROM
            tb_post
        WHERE 1=1
        AND
            board_id = #{id}
        AND
            delete_yn = 0
    </select>

</mapper>
