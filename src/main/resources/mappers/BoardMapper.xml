<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.mapper.BoardMapper">

    <!-- tb_board 테이블 전체 컬럼 -->
    <sql id="boardColumns">
          id
        , title
        , content
        , writer_id as writerId
        , created_date as createdDate
        , updated_date as updatedDate
    </sql>


    <!-- 게시글 리스트 조회 -->
    <select id="findAll" resultType="com.study.domain.BoardDTO">
        SELECT
            id,
            title,
            content,
            writer_id as writerId,
            created_date as createdDate,
            updated_date as updatedDate
        FROM
            tb_board
    </select>

    <select id="findById" parameterType="long" resultType="com.study.domain.BoardDTO">
        SELECT
            id,
            title,
            content,
            writer_id as writerId,
            created_date as createdDate,
            updated_date as updatedDate
        FROM
            tb_board
        WHERE 1=1
        AND id = #{id}
    </select>





</mapper>
